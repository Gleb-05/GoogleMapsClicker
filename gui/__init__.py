"""
Defines basic operations in google maps interface, grouped by where they happen.

Basic operations are intentionally simple, so it's possible to utilize them in very different contexts.
The context is defined by user intentions. Right now, it's "extract place info" and "get area image".
"""
